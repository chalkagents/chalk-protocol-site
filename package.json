{
  "name": "chalk-protocol-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
